# cyber_automate

# Git Repository
- Enable Dependabot
- Enable CodeQL
## git-crypt
~ git-crypt init
~ git-crypt export-key path/to/key

# virtualenv
~ virtualenv venv
~ source venv/bin/activate
~ pip install -r requirements.txt

# Project Structure
